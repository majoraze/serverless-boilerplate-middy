config:
  packageIndividually: true
  profile: mobly-dev

tags:
  departament: TI
  project: services
  service: ${self:service}
  environment: ${self:custom.stage}

environment:
  MONGO_URI: ${ssm:/db/mongo_uri}
  STAGE: ${self:custom.stage}
  # ACCOUNT_ID: '#{AWS::AccountId}'
  SERVERLESS_SERVICE: ${self:service}
